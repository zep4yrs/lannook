name: Publish Desktop Installers

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing GitHub Release tag to receive the installers
        required: true
        default: v26.2.1
        type: string
      platform:
        description: Platform group to build
        required: true
        default: all
        type: choice
        options:
          - all
          - macos
          - linux

permissions:
  contents: write

jobs:
  build-and-upload:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS Intel
            platform: macos
            os: macos-14
            rust_target: x86_64-apple-darwin
            bundles: dmg
            assets: src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
          - name: macOS Apple Silicon
            platform: macos
            os: macos-14
            rust_target: aarch64-apple-darwin
            bundles: dmg
            assets: src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
          - name: Linux x64
            platform: linux
            os: ubuntu-22.04
            rust_target: x86_64-unknown-linux-gnu
            bundles: appimage,deb
            assets: src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/appimage/*.AppImage src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/deb/*.deb

    steps:
      - uses: actions/checkout@v4
        if: inputs.platform == 'all' || inputs.platform == matrix.platform

      - uses: actions/setup-node@v4
        if: inputs.platform == 'all' || inputs.platform == matrix.platform
        with:
          node-version: 22
          cache: npm

      - name: Install frontend dependencies
        if: inputs.platform == 'all' || inputs.platform == matrix.platform
        run: npm ci

      - name: Install Rust
        if: inputs.platform == 'all' || inputs.platform == matrix.platform
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Install Linux build dependencies
        if: (inputs.platform == 'all' || inputs.platform == matrix.platform) && runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            fakeroot \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            patchelf

      - name: Build installers
        if: inputs.platform == 'all' || inputs.platform == matrix.platform
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npx tauri build --target ${{ matrix.rust_target }} --bundles ${{ matrix.bundles }}

      - name: Attach installers to the existing Release
        if: inputs.platform == 'all' || inputs.platform == matrix.platform
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          shopt -s nullglob
          assets=(${{ matrix.assets }})
          while IFS= read -r -d '' signature; do assets+=("$signature"); done < <(find src-tauri/target -type f -name '*.sig' -print0)
          if [ ${#assets[@]} -eq 0 ]; then
            echo "No installer assets were produced."
            exit 1
          fi
          gh release upload "${{ inputs.tag }}" "${assets[@]}" --clobber --repo "$GITHUB_REPOSITORY"
